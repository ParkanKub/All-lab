#include <stdio.h>

int main() {
    char Input[100];
    char Output[100];
    char Process[100];
    scanf("%s %s %s", Input, Process, Output);

    printf("Input: %s\n", Input);
    printf("Process: %s\n", Process);
    printf("Output: %s\n", Output);

    return 0;
}