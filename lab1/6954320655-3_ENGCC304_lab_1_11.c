#include <stdio.h>

int main() {
    char OS[100];
    char Editor[100];
    scanf("%s %s", OS, Editor);
    printf("OS: %s\n", OS);
    printf("Editor: %s\n", Editor);
    printf("Ready to code!");
    return 0;
}